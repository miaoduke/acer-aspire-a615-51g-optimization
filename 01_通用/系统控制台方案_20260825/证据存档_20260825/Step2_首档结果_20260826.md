# Step 2 首档实测结果 — -9.8mV 成功 (2026-08-26)

## 历程 (一路排障)
1. 火绒删 WinRing0 驱动 (TS 释放到 %TEMP%\ThrottleStop.sys, 信任区只加 TS 目录管不到) → 信任区补加该文件 ✅
2. "Could not change service config" 持续 → 服务数据库残留项指向死路径 → `sc delete ThrottleStop` (第一次管理员窗口静默未删成, 第二次成功) ✅
3. 手动 `sc create + start` → **STATE: RUNNING** — 驱动加载成功 ✅
4. TS 免责声明 → FIVR 完整打开 → **MSR 0x150 读写通, 微码 0xE0 不拒** ✅

## 首档结果
| 项 | 值 |
|---|---|
| CPU Core Offset | **-9.8mV** (125mV 量程最接近 -10 刻度) |
| CPU Cache Offset | **-9.8mV** (Core+Cache 同步) |
| 证据 | Voltage ID 1.1024 → 1.0753V 实时下移; TS 主界面 PKG 8.6W 空闲, 8 核 46-51°C |
| Save | immediately (防退出丢失) |

## 稳定性初判 (降压后 13 分钟)
- WHEA/Kernel-Power/异常重启: 无 ✅
- 应用崩溃: 14:33 两条 (降压设置前, 火绒混乱期, 无关)
- TS 进程存活 ✅

## 注意事项
- **TS 必须常驻** (退出=降压失效; Save immediately 只是存配置, 运行时需 TS 持续应用)
- Speed Shift EPP 显示 128 (TS 口径) vs 我们 EPP=40 — 口径差异, 待核对, 不影响降压
- 观察期: 明日复查事件日志终判; 无异常则阶梯 -25 → -50 (Linux 舒适档) → 目标 -100
- 红线: -105mV 死机; 任一档不稳回退上一档

## 环境变更记录
- 内存完整性: 关 (HVCI 停止, VBS 壳保留) — 恢复方法见 15 文档
- 火绒信任区: Temp\ThrottleStop.sys + TS9.7.3 目录
- 服务: ThrottleStop (kernel, demand, 指向 Temp\ThrottleStop.sys)
