# Linux 控制台 Bug 修复档案

> 机型: Acer Aspire A615-51G | 控制台: `05_控制台_Linux/系统控制台_最新_20260822/`（GTK3/Python）
> 整理日期: 2026-08-31 | 来源: 静态代码审计 + 三轮动态测试
> 状态: 全部已修复并实机验证

---

## 一、静态代码审计发现（2026-08-31 上午，9 项）

| # | 问题 | 位置 | 影响 | 修复 |
|---|---|---|---|---|
| 1 | **`reboot_system()` 双重 sudo** | controller.py:159 | 实际执行 `sudo -n sudo /usr/sbin/reboot`，白名单只放行 reboot 本身 → 两个"立即重启"按钮**必失败** | 改为 `_run(["/usr/sbin/reboot"])`（_run 内部补 sudo）|
| 2 | **`_on_pl_apply` 孤儿代码**引用未定义的 `v` | ui_advanced.py:420-426 | 每次点 PL"应用"都额外弹"温度墙设置失败: name 'v' is not defined" | 删除 7 行残留 |
| 3 | **`refresh_cam_state` 尾部孤儿块**引用不存在的 `svc_list` | ui_advanced.py:521-538 | 摄像头操作后二次 run_async 触发 AttributeError | 删除（该功能由 ui_maintenance 实现）|
| 4 | **`_read_int` 内嵌 72 行不可达代码** | ui_advanced.py:660-733 | 性能日志查看器函数头丢失 → 72 行 UI 代码不可达 | 删除（完整版在 ui_maintenance）|
| 5 | **`_open_scene_config` 尾部挂 80 行功耗排行** | ui_advanced.py:964-1043 | 打开场景参数对话框混入无关的功耗排行控件 | 删除（完整版在 ui_maintenance）|
| 6 | **场景保存用 `sudo tee`（不在白名单）** | ui_advanced.py:929 | `tee`/`bash -c` 不在 sudoers → 保存间歇失败 | 改为直接写用户文件（属主本就是用户），GT 频率降级提示 |
| 7 | **SERVICES 服务名错误** | controller.py:81 | `intel-undervolt` 不存在（真机是 `undervolt`）；描述"-100mV"过时 | 改名 + 描述改 -80mV + 补 4 个真机服务（共 10 项）|
| 8 | **导航漏"说明文档"项** | console.py:130 | 591 行页面不可达 | 补导航项（→ 暴露 GTK4 bug，见动态测试 #1）|
| 9 | **`sudo_ok()` 与 install.sh 不一致** | controller.py:393 | 检查 `99-thermal-ctl`/`99-acer-battery-temp`（历史手动文件），新机器只跑 install.sh 时不存在 → 误报"白名单未配置" | 对齐 install.sh 生成物（system-console / system-console-thermal），兼容历史命名 |

**附带清理**：`undervolt` 生态显示改解析实际配置（原硬编码 `-50mV`，改 -80 后永远显示"在配"）；
`src/gui/` 805 行死代码移入 `99_存档_只读/移出死代码_20260831/`。

---

## 二、动态测试发现（2026-08-31，5 项）

### 测试发现 1：补导航入口导致启动崩溃（GTK4 API 残留）⭐

**现象**：修完导航漏项后启动即崩：
```
File ".../ui_docs.py", line 152, in _build_overview
    box.pack_start(self._h2("设计原则"))
TypeError: Gtk.Box.pack_start() takes exactly 5 arguments (2 given)
```

**根因**：`ui_docs.py` 写的是 **GTK4 API**（`pack_start(child)` 1 参），项目用 **GTK3**（需 `(child, expand, fill, padding)`）。该页"不可达"的真正原因是**本来就跑不起来**——当初很可能因此被移除入口，我却误以为只是漏加导航。

**修复**：54 处 pack 调用补参数（AST 精确定位 + 单行判定后批量转换）。

**踩坑**：AST `col_offset` 是 **UTF-8 字节偏移**，`len(line)` 是字符数——中文 3 字节/字导致偏移错位，脚本首次运行 IndexError。改用「行尾最后一个 `)`」定位后成功。

**结果**：`DocumentationPage` 实例化成功（2 子控件），六页面全部实例化回归通过。

### 测试发现 2：温度源错误（最严重）⭐

**现象**：采集温度显示 27.8°C，但 CPU 真实温度 59°C（相差 31°C）。

**根因**：`collector.py:17` 主温度源指向 `thermal_zone0`（ACPI **机壳/环境**温度），CPU 封装温度在 `thermal_zone2`（x86_pkg_temp）。

**后果**：
1. 总览页/场景页温度卡显示环境温度而非 CPU 温度
2. **78°C 高温告警永远不触发**（CPU 到 78°C 时采集值才约 47°C）—— 告警机制形同虚设

**修复**：改为 CPU 封装优先（`thermal_zone2`），缺失时回退 `thermal_zone0`。

**结果**：复测采集 55.0°C vs 真机 56.0°C（±1°C 正常波动），告警链路恢复。

### 测试发现 3：单实例失效（锁 PID 漂移）⭐

**现象**：重复启动时未拦截，产生多个实例。

**根因**：启动脚本 `exec python3 console.py 2>&1 | tee -a log` —— 管道使 bash 创建**子 shell**，`exec` 替换的是子 shell 而非本进程，导致 `$$` 写入锁文件的 PID ≠ 实际 python 进程。

**修复**：改为「后台 tee + 前台 exec（无管道）」→ PID 保持不变，锁文件精确。

**结果**：锁文件 PID 精确等于 python 进程，单实例拦截提示正确。陈旧锁（进程已退出）自动忽略，`--force` 可绕过。

> ⚠️ 测试方法教训：首次用 `timeout` 启动，进程已被终止 → 锁陈旧被正确忽略 → 误判"拦截失效"。改让进程真正持续运行后正常。**测试方法本身要正确。**

### 测试发现 4：`bench_once()` 双重 sudo（基准测试完全不可用）⭐

**现象**：点"运行基准测试"报 `sudo: 需要密码`，迭代数=0。

**根因**：`_run(["sudo", "-n", SCENE_SCRIPT, "bench"])` —— 与 `reboot_system()` **完全同类**的双重 sudo，0831 上午修 reboot 时按关键词搜索**遗漏了此处**。

**修复**：去掉显式 `"sudo", "-n"`（_run 会补）。

**结果**：`bench_once()` 1558.3 万/s；完整 EMP 协议 1422.0 万/s（5 样本无离群）。

> ⚠️ 教训：同类 bug 会分布在不同函数，必须**按调用点逐个核对**而非只搜关键词。

### 测试发现 5：`fan_boost` 防御不足 ⭐

**现象**：`controller.fan_boost()` 与 `thermal_ctl.sh` 分支仍可被调用。

**根因**：fan_boost 是**死机 #4 的直接操作**（ACPI→VFN→EC 写入），项目铁律**永久禁用**——但此前仅靠"UI 不显示按钮"维持，任何误调用都会触发 EC 写入。

**修复**：加**硬拦截**：
- 全局开关 `FAN_BOOST_ALLOWED = False`（默认拒绝）
- `fan_boost(on, force=False)`：默认返回错误 + 安全替代说明（TCC/PL 限制热源，EC 自动调风扇）
- `force=True` 才执行（仅应急/debug）
- 另修：新增 force 参数时**先漏了改签名**，已自查对齐

**结果**：默认调用 rc=1 拒绝，未触碰 EC。

---

## 三、修复结果汇总

### 验证覆盖（三轮动态测试，全部实机通过）

```
采集层 / 六页面渲染+刷新 / PL·TCC·maxperf 写读恢复
USB·WiFi·摄像头 开关可逆 / 场景切换三参数同步 / 场景参数保存
基准测试（单次+EMP）/ M3 进入退出（DC 完整闭环）
GPU 切换全流程（两次重启双向生效）/ 插电自动恢复
启动器（预检/日志/单实例）/ 一键重启命令构造
```

### 当前系统状态（修复后，一切正常）

```
降压: -80.08mV（观察期至 09-03）| PL: 25W/25W | C-state: 4
prime: intel | MCE: 0 | 场景: ac-perf（AC）
服务: cpu-power-limit / turbo-enable / undervolt / undervolt-resume /
      acdc-profile / thermal-guard / rasdaemon /
      uv-safeguard（异常关机回退 -50mV）/ uv-daily-check（每日巡检）
```

### 未实机验证项

| 项 | 状态 |
|---|---|
| GPU 切换端到端 | ✅ 已完成（两次重启实机验证）|
| 一键重启按钮本身 | ⚠️ 命令构造已验证匹配 sudoers，但未实际点（会重启中断）；逻辑与 GPU 测试用的重启同源 |
| M3 | ✅ 已完成（DC 完整闭环）|

---

## 四、方法论沉淀（比 bug 本身更值钱）

1. **`import` 成功 ≠ 类能实例化** —— UI 类必须实例化验证（测试发现 1）
2. **静态审计发现不了运行时 bug** —— 温度源、单实例、fan_boost、bench_once 全是动态测试暴露（测试发现 2-5）
3. **同类 bug 分布在不同函数** —— 必须按调用点逐个核对，不能只搜关键词（测试发现 4）
4. **测试方法本身要正确** —— `timeout` 测单实例导致锁陈旧误判（测试发现 3）
5. **AST `col_offset` 是字节偏移** —— 中文环境用「行尾最后字符」定位更稳（测试发现 1）
6. **修 bug 前先看它在掩盖什么** —— Makefile KDIR（0830）、GTK4 页面（0831）都是补一处暴露另一处
