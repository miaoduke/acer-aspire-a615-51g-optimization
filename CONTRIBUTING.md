# Contributing — 贡献指南

感谢你关注本项目。这是个人业余的电源/性能优化研究项目（MIT）。在提交任何内容前，请务必阅读以下约束。

## 禁止提交的内容（Hard Rules）

- ❌ **私有 / 个资**：真实用户名、主机名、MAC/IP、序列号、密码/口令、个人数据路径（这类内容应替换为 `<USER>` / `<HOSTNAME>` 占位符）。
- ❌ **OS 原厂商 / OEM 逆向原始内容**：来自 ACER/微软/英伟达等厂商的闭源二进制、反编译/解包/抓包载荷、固件转储、注册表/系统转储原始文件。
- ❌ **第三方专有二进制**：`*.exe`、`*.dll`、`*.bin` 等随包二进制（`*.dll`/`*.exe`/`PowerSettingsExplorer.exe`/`ThrottleStop` 等已被 `.gitignore` 排除）。
- ❌ **系统日志 / 会话库**：`*.log`、journalctl/dmesg 输出、`opencode` 数据库、快照目录（已排除）。

## 需要遵循的约定

- **脱敏优先**：任何新文档/脚本出现本机路径、用户名、UUID 时，一律用占位符，并在「关于脱敏」说明中登记。
- **许可边界**：不要将 GPL-2.0（`acer-wmi-battery`）源码混入 MIT 主代码树；第三方组件修改请遵守其各自许可，并同步更新 [THIRDPARTY.md](THIRDPARTY.md)。
- **安全报告**：漏洞请走 [SECURITY.md](.github/SECURITY.md) 的私有通道，勿公开贴凭据。
- **结论可追溯**：修改结论时请保留历史标注，勿覆盖既有事实源（见 `03_Linux/性能优化方案_20260822/00_交接手册_重装后启动.md`）。

## 提交流程

1. Fork 并提交到独立分支。
2. 提交前本地自检：`git check-ignore` 确认无私有文件，并跑一遍敏感词扫描（hackdale/d6150/UUID/主机名/MAC）。
3. 提交信息：`类型(scope): 摘要`（如 `docs(desense): 新增脱敏说明`）。
4. 开 PR 并说明改动动机与测试情况。