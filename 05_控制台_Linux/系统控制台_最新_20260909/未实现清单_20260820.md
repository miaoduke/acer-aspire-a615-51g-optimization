# 未实现 / 放弃项清单（穷尽调研记录）

> 全部经实测或权威资料调研后**有意放弃**或**待条件满足**的项目。已实现项见"生态优化状态"。
> 更新日期：2026-08-20

## 一、放弃项（有明确依据，不实现）

| # | 项 | 调研结论 | 放弃原因 |
|---|-----|---------|---------|
| 1 | **PSR（面板自刷新）** | 内核 2020 年因 Dell 全系面板闪烁改默认禁用；本机面板支持性未知 | 0.5-1W 收益 vs 花屏/闪烁风险，不划算 |
| 2 | **PL2 时间窗口调整** | 实测 2440µs→10000µs：bench 1394 vs 1601 万/s（**-13%**） | 实测负收益，恢复原值 |
| 3 | **Turbo ratio override（MSR 0x1AD）** | 本机频率受 PL1=25W 功耗墙主导（4 核全频远超 25W） | 功耗墙下无收益 |
| 4 | **swappiness 激进调高**（100-133） | zswap 场景社区建议，但本机无内存压力证据 | YAGNI，压力出现再调 |
| 5 | **page-cluster 调整** | 只影响 zram（块设备），对 zswap 无影响（chrisdown 权威） | 无效项 |
| 6 | **M3 物理键监听** | 解谜：acer-wmi 0x23=KEY_PROG3(202)。**但本机键盘无 M3 键**（已确认） | 硬件不存在，监听无意义，已移除 |
| 7 | **健康模式（health_mode）** | **dmesg 实锤：固件 WMI 不支持**（`WMI battery status call returned unexpected length 4` + `health/calibration query not supported by firmware`）——非白名单问题 | 固件层面无解；保留只读温度（无害） |
| 8 | **风扇强制控制** | **实锤：fan_boost（ACPI→VFN 共享内存→EC 写入）即四次死机之一（死机4）的直接操作**；2025-02 acer-wmi 风扇控制仅配给 Predator 游戏本（ACER_CAP_PWM quirk），本机非游戏本无此能力；无独立风扇芯片、无 ACPI fan 之外接口 | **永久禁用**（死机史铁律）。安全替代：TCC/PL 功耗墙控制热源（EC 自动风扇响应） |
| 9 | **充电 80% 上限（Care Center）** | Windows 分区检查：**未安装 Care Center，从未设置过**；Linux 侧 health_mode 固件不支持（dmesg 实锤：WMI 调用返回异常长度） | 唯一途径：Windows 装 Care Center 设一次（写入 EC 后 Linux 也遵守），用户未做 |
| 10 | **stress-ng / 高负载测试** | **实锤：死机 2/3 均涉 stress-ng 满载**（+EC 直读 / +/dev/mem 读）——满载是最强触发器 | **永久冻结**。安全替代：① 自研 bench（8 线程 LCG，多次实测安全，数秒级）② 受控协议（限 PL1≤10W + 限时≤60s + 温度监控 + 纯 CPU 模式） |
| 11 | **刷新率联动（60/48Hz）** | eDP-1 EDID **只支持 60.03Hz** + 强制 48Hz 实测 `Configure crtc 0 failed`（xrandr） | 面板/驱动双重拒绝，硬件不支持 |
| 12 | **FBC（帧缓冲压缩）** | 参数加载成功但驱动无 FBC 状态文件（debugfs 无 i915_fbc_status）——**实测闭环** | 本机面板不支持，已回滚参数 |

## 二、待观察项（已落地，观察期验证）

> ✅ **2026-08-29 实测更新**：C-state 已从 6 收敛至 **4**（`/etc/default/grub` 与
> `/sys/module/intel_idle/parameters/max_cstate` 双向一致），观察期早已结束（0816→0829 无异常），
> 该项由"待观察"转为"长期稳定"。演进：9→1→9(**失败**, 1天4死)→1→6→**4**。

| # | 项 | 状态 | 回滚条件 |
|---|-----|------|---------|
| 1 | **C-state=4（intel_idle.max_cstate=4）** ✅ | 2026-08-29 实测确认（原记 6 已变更） | **勿回退至 9**（深 C 全开曾 1 天 4 死）；异常则退 1 |
| 2 | **hwp_dynamic_boost=1** | 已开启（acdc-profile v5 设置） | 官方文档：gov=performance 时无效，无副作用预期 |
| 3 | **zswap.shrinker_enabled=1** | sysfs + GRUB 持久化 | 理论上无副作用 |

## 三、条件项（需特定条件才做）

| # | 项 | 触发条件 |
|---|-----|---------|
| 1 | Care Center 80% 充电上限 | 用户进 Windows 安装设置（一次性，EC 持久） |
| 2 | 电池健康采样趋势判断 | 周-月尺度，≥2 次采样后看趋势线 |
| 3 | 离电放电率精确对比 nvidia 模式 | 需 nvidia 模式 + 拔电实测（当前 intel 模式已证更优：分析性结论） |
| 4 | **-100mV 降压探索** | 需 -80mV 观察期(3-5天)通过。**-105mV 有死机 #6 史，建议止步 -100mV**；必须用 `uv_sweep_v2.sh` 带防护 |

## 三·补 2026-08-30 追加（DC 满载红线跨越 + 重要归因修正）

| # | 项 | 结论 | 状态 |
|---|-----|------|------|
| 1 | **DC 满载（历史红线）** | 2026-08-30 首次安全跨越：-50mV 与 -80mV 均在 DC 下跑完 L1(2核)/L2(4核)/L3(8核) 渐进加载，**MCE 全 0、全程未死机**。 | ✅ **红线已解除** |
| 2 | ⚠️ **重要归因修正：DC 性能受限的真因** | 历史结论"DC 供电限制性能"**不成立**。实测：<br>· DC + bat-perf（PL1 15W, Turbo 开）= **1700.9 万/s**，几乎追平 AC 的 1729.5 万/s<br>· DC + bat-save（PL1 10W, Turbo **关**）= 623.5 万/s<br>→ 差距主因是**场景配置**（bat-save 压 PL1 到 10W 并由 turbo-guard 主动关 Turbo），与电池供电能力无关。 | ✅ 已修正 |
| 3 | ⚠️ **场景脚本注释过时** | `场景管理.sh` 的 bat-perf 注释称"受固件限制, 实际 PL1≈8W"。实测 DC 下可写入并跑满 **15W**，注释低估。 | ℹ️ 待更新脚本注释 |
| 4 | **降压 MSR 不受 EC 重置影响** | 历史实证"拔电瞬间 EC 重置 MSR"**仅影响 Turbo(0x1A0)**，不波及降压(0x150)。实测拔电瞬间/+10s/+30s/+45s 降压值恒为 -80.08mV。这解释了为何 turbo-enable 需 30s 守护而 undervolt 用 oneshot 即可。 | ✅ 已澄清 |
| 5 | **uv-safeguard 安全网** | 新增服务：开机检测上次是否异常关机(硬死机/掉电)，若是自动回退 -50mV + 写标记 + 桌面通知。防止"死机→重启→又应用深降压→再死机"循环。 | ✅ 已部署 |
| 6 | **uv-daily-check 每日自检** | 新增 timer（每日 10:00 + 开机 3 分钟补跑）：巡检 7 项（MCE 增量 / 降压值偏差 / 异常关机 / 关键服务 / safeguard 标记 / 电池温度可读性 / 内核变化）。**正常静默，异常才通知**——因 -105mV 的教训是"验证通过后数天才死机"，单次测试不足以定论。已双向验证：正常退出 0、人为改 -60mV 时精确检出 20.43mV 偏差。 | ✅ 已部署 |
| 7 | **数据盘自动挂载（重启审计发现）** | 2026-08-30 重启后发现数据盘(WS, NTFS)【未挂载】——它**从未写入 fstab**，每次重启都需手动挂载，导致项目归档与 `sync_to_ws1.sh` 等工具重启即失效。已追加 fstab 条目（UUID 方式 + `nofail`，findmnt 校验 0 错误）。 | ✅ 已修复 |
| 8 | **挂载点 WS/WS1 漂移** | 盘标签为 `WS`，但 udisks2 在重名时自动加序号 → 有时是 `WS1`。此前 `sync_to_ws1.sh`、`dc_load_test.sh` 硬编码 `WS1`，挂载点一变就失效。已改为**自动探测**（依次尝试 WS/WS1/WS2，兜底全盘搜索）。 | ✅ 已修复 |
| 9 | ntfs3 vs ntfs-3g | 内核 7.0.0-30 自带 `ntfs3` 原生驱动（性能优于 FUSE 的 ntfs-3g），当前用 ntfs-3g 工作正常。**属优化非缺陷**，切换需重挂验证且有数据风险，暂不改。 | ℹ️ 可选优化 |

## 四、已落地生态（摘要，GUI 高级页可查）

AC/DC 场景自动切换 · hwp_dynamic_boost · **C-state=4**（原记 6）· zswap+shrinker · swap 4G · MX150 D3cold · journald 限容 · 启动优化（wait-online/fwupd）· smartd 双盘自检 · hdparm 机械盘 · WMI 电池温度（acer-wmi-battery，已 DKMS 化）· 校准指引 · USB/WiFi/ALPM/ASPM/蓝牙省电 · **降压 -80mV**（原 -50mV，2026-08-30 定稿）· **uv-safeguard 异常关机回退** · **uv-daily-check 每日静默自检**
> ⚠️ 2026-08-21 已移除：亮度联动（交还用户控制）。本行原含"亮度联动"为过期表述，已删。

## 五、2026-08-20 追加（软硬件结合调研轮）

| # | 项 | 结论 | 状态 |
|---|-----|------|------|
| 1 | **undervolt（CPU/GPU/缓存降压）** | Plundervolt **未锁定**本机 BIOS；-49.8mV 三域生效。 <br>✅ **2026-08-30 推进到 -80mV 并持久化**（实测 -80.08mV）—— 「观察后可试 -80mV」这条已完成。五项场景验证全过，详见 `性能优化方案_20260822/降压实验记录_-80mV_20260830.md` | ✅ **定稿 -80mV**（观察期 3-5 天中） |
| 2 | **GPU GT 频率联动** | i915 RP0=1100/RPn=300MHz 可写（root）；离电限制突发功耗 | ✅ 已落地（acdc-profile v6：AC=1100/DC=700，可配置） |
| 3 | 刷新率联动 | EDID 仅 60Hz | ❌ 硬件不支持 |
| 4 | FBC | 面板不支持 | ❌ 放弃 |
| 5 | 键盘背光 | 无 kbd_backlight 设备 | ❌ 无此项 |

## 六、2026-08-20 追加（风扇 EC / stress-ng 穷尽轮——实锤升级）

| # | 项 | 穷尽结论 | 最终裁决 |
|---|-----|---------|---------|
| 1 | **风扇 EC 控制** | 网络：2025-02 acer-wmi 风扇支持（SetGamingFanSpeed）**仅 Predator 游戏本**（ACER_CAP_PWM quirk PH16-72/PT14-51/PHN16-72）；本机非游戏本无能力标志。本地：hwmon 无 fan、ACPI 无独立 _FAN 对象、acer-wmi 无风扇 sysfs | **永久禁用**（fan_boost 即死机 4 操作，铁律） |
| 2 | **stress-ng** | 已安装但**永久冻结**（死机 2/3 均涉其满载）；安全替代 bench 已在用 | 维持冻结 |
| 3 | **安全替代（新）** | ① 温度→PL 功耗墙自动限流（软硬件结合，不碰 EC）② 受控压力协议（限 PL1+限时+温度监控+纯 CPU 模式）③ bench 已覆盖日常测试 | ✅ ① 已实现（thermal-guard.service，测试通过 56°C 触发 25→15W 并恢复） |

## 七、2026-08-20 追加（nvidia 能力穷尽轮）

| # | nvidia 能力 | 本机 MX150 | 电源联动 |
|---|------------|-----------|---------|
| 1 | **PowerMizer 模式**（GPUPowerMizerMode 0/1/2：自适应/性能/自动） | ✅ 支持（X11 有效） | ✅ **已联动**（acdc-profile v7：AC→1 性能 / DC→0 自适应省电；intel 模式静默跳过） |
| 2 | nvidia-smi power limit（-pl） | ❌ 无（社区确认 MX 全系） | — |
| 3 | GPU 时钟锁定（-lgc/-ac） | ❌ Turing+ 才有 | — |
| 4 | 超频/OC | ❌ NVML 不支持 | — |
| 5 | 风扇控制 | ❌ 无独立风扇 | — |
| 6 | Dynamic Boost | ❌ 需新硬件 | — |
| 7 | persistence mode | 存在但保持驱动常驻（省电无益） | 不推荐 |
| 8 | prime 切换 | ✅ intel/nvidia/on-demand | ✅ 已做（intel = D3cold 断电 0W 全局最优） |
| 9 | GPUCurrentPerfLevel/AdaptiveClockState | ✅ 只读监控 | 监控用 |

**联动评估**：当前 intel 模式 MX150 断电为全局最优（0W）；nvidia 模式时 DC 下 PowerMizer=0 自适应省 0.5-1W——**联动已在代码，待切 nvidia 模式实测验证**。nvidia 侧已穷尽。

## 八、2026-08-21 追加（内核升级修复 + 电源接管）

| # | 项 | 处置 |
|---|-----|------|
| 1 | **内核 7.0.0-28→29 升级** | acer-wmi-battery 模块未随内核更新→加载失败；**已针对 7.0.0-29 重新编译安装 + depmod + 恢复自动加载**（modules-load.d，collector 60s 节流降 SMI 风险）<br>🔴 **2026-08-29 复核（内核 28→30，故障已发生）**：当天 12:29 关机、12:30 以 **7.0.0-30-generic** 启动。审计期间先观察到"内核=28、/boot 仅 28"（用户确认该回退为**无意**），随后系统在 12:30 升级到 30 —— **该模块因未 DKMS 化而失效**：`modprobe: FATAL: Module acer_wmi_battery not found in /lib/modules/7.0.0-30-generic`，`/sys/bus/wmi/drivers/acer-wmi-battery/` 消失，**电池温度功能当前失效**。<br>🔴 **根因**：模块**从未注册 DKMS** —— `dkms.conf` 存在且 `AUTOINSTALL=yes`，但 `/var/lib/dkms/` 下只有 nvidia，一直靠手动 `make install`；内核未锁定（`apt-mark showhold` 空）。**这是同一故障第三次发生**（0818 / 0821 / 0829）。<br>✅ **已修复（2026-08-29 用户执行 `register_dkms.sh`）**：`dkms status` →
> `acer-wmi-battery/1.0, 7.0.0-30-generic: installed`；vermagic 匹配 30；**电池温度恢复 30.9°C** ✅；
> 模块经 **MOK 签名**（此前手动版未签名，对 Secure Boot 是改进）。<br>🔧 **修复中发现并修掉的两个隐藏 bug**：
> ① `Makefile` 硬编码 `$(shell uname -r)`、忽略 `dkms.conf` 传入的 `KDIR` → 若只注册 DKMS 而不修此点，
> 升级后未重启时会按旧内核编译却装进新内核目录，vermagic 不匹配 → **故障会第四次发生**；
> ② `kernel_guard.sh` 的 KDIR 路径多写一截（`-generic-generic`，路径不存在），此前因 ① 被掩盖，
> 修 ① 后会暴露为 repair 失败，已同步修正。构建日志已证实 KDIR 生效：
> `make KERNELRELEASE=7.0.0-30-generic KDIR=/lib/modules/7.0.0-30-generic/build`。<br>✅ **30 下核心优化栈全部存活**（服务化设计的价值）：PL 25W/25W、no_turbo=0、C-state=4、6 服务全 active/enabled，仅电池温度模块受损且现已修复。 |
| 2 | **电源模式接管** | 用户决定由控制台完全接管电源管理：PPD 保持 masked（系统电源模式选项消失为预期，不处理）；场景服务全 enabled 保证重启自启 |
| 3 | **亮度用户自主** | acdc-profile 移除亮度联动（set_backlight/BRIGHT 全删）；禁用 Cinnamon `idle-brightness`（-1，闪屏主因）——亮度完全用户控制 |

## 九、2026-08-21 追加（内核 7.0.0-29 升级重新检测轮）

| # | 项 | 结论 | 状态 |
|---|-----|------|------|
| 1 | **RAPL 多域功耗分解**（core/uncore/dram） | 7.0 新增能力，实测可用（10.5W=7.1核心+2.7缓存+1.6内存） | ✅ **已落地**（dashboard 功耗分解行） |
| 2 | PSR2 sel-fetch | 8 代 UHD 620 支持 PSR2 但 Kaby Lake-R 有死机bug（Bug 112159） | ❌ 放弃（风险） |
| 3 | Panel Replay / DC3co | 需 Gen12+（8 代 Gen9.5 无此能力） | ❌ 硬件不支持 |
| 4 | intel_pstate 新参数 | energy_efficiency_preference 8 代无；hwp_dynamic_boost 已用 | 无新项 |
| 5 | psys 平台功耗域 | 能量计数异常（固件未实现） | ❌ 放弃 |
| 6 | kernel_guard（内核变动防护） | 检测 17 项 + 一键修复（模块重编译/服务重启/dkms） | ✅ **已接入 GUI**（新生态项） |
## 十、铁律更新（2026-08-22 快照事故后）

| # | 新铁律 | 触发条件 | 工具 |
|---|--------|---------|------|
| 1 | **L1 系统级修改前必须快照** | GRUB/内核/批量服务/驱动操作 | `sudo bash backend/snapshot.sh create "说明"` |
| 2 | **L2 高风险实验前必须快照+记录回滚** | undervolt/C-state/EC 相关 | 同上 |
| 3 | **控制台更新后同步 WS1**（带日期目录，留 3 份） | 每次 py/sh 文件变更批次完成 | `bash sync_to_ws1.sh`（已挂入备份脚本尾部自动执行） |
| 4 | **AI 对话原文 md 导出 + 图片留存** | 每阶段任务结束 | `python3 export_session.py` → 会话记录/ |
| 5 | **跨机适配先跑适配测试** | 重装/新机器部署 | `sudo bash backend/adapt_test.sh apply` |

### 快照分级原理
- Timeshift btrfs CoW：创建秒级、空间只随差异增长——低成本低风险高
- 只保护 @（系统）；@home 靠 WS1 同步双层保障
- 低价值快照会稀释回滚点——不搞"每次都拍"

### 跨机可移植性（本次新增基础设施）
- `src/core/hardware_probe.py`: 硬件探测（DRM 卡/电池/温度区/RAPL 自动发现 + 能力矩阵）
- `backend/adapt_test.sh [apply]`: 适配测试向导（检测→报告→写 hardware_profile.json）
- `data/knowledge/`: 经验库（undervolt sweep/bench 对比/固件边界清单/移植检查单）
- `backend/snapshot.sh`: 快照管理封装

### 快照科学验证法（用户提出，待执行）
恢复干净快照 → 跑适配向导 → 验证从零建栈流程 → 快照切回。
确定性环境重现，适合验证重装场景。执行时机：下次计划内重启时一并做。
