# profiles/user_custom/tuned.conf — 用户自定义示例

[main]
summary=用户自定义 — 模板
include=balanced
version=1

[cpu]
# 修改以下字段创建你自己的 profile
# governor=performance
# energy_performance_preference=performance
# pl1_watts=20
# turbo=1

# 添加其他段（如 [plugin]）可调用自定义 plugin
